declare const config: {
    db: {
        host: string;
        port: number;
        user: string;
        password: string;
        database: string;
    };
    http: {
        port: number;
    };
};
export default config;
