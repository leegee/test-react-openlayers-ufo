{
    "name": "@ufo-monorepo-test/config",
    "version": "1.0.0",
    "description": "Configuration read from .env, with defaults for localhost",
    "main": "./build/config/index.js",
    "types": "./build/config/index.d.ts",
    "type": "module",
    "scripts": {
        "build": "tsc --build",
        "prebuild": "npm run clean",
        "clean": "rm -rf build/*"
    }
}