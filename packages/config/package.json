{
  "name": "@ufo-monorepo/config",
  "version": "1.0.0",
  "description": "Configuration read from .env, with defaults for localhost",
  "type": "module",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "prebuild": "npm run clean",
    "clean": "rimraf build && rimraf tsconfig.tsbuildinfo"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.2.3"
  }
}
